# 週次メンテナンス手順(株価・優待データの鮮度チェック)

広告承認まで自動更新(J-Quants有償プラン)を保留しているため、
週1回、以下を実行して手動でデータの鮮度を保つ。

## 手順

1. `src/lib/yutai-data.ts` の YUTAI_LIST から、`lastVerified` が最も古い順に
   上位25銘柄を抽出する
2. 抽出した25銘柄それぞれについて、Web検索で以下を確認:
   a. 現在の株価(取得日が明記された証券会社サイト等を優先)
   b. 株主優待制度が継続しているか(改悪・廃止・変更の有無)
3. 株価が確認できた銘柄は `approxInvestment = 株価 × minShares` で再計算し、
   `lastVerified` を確認日に更新
4. 優待制度に変更があった銘柄は、`description`・`annualValue`・`rightsMonths` 等を
   実態に合わせて修正
5. 廃止が確認された銘柄は、削除せず一旦 `dataQuality` を `"要確認"` 等にして
   報告に明記する(即削除しない。雅英さんが内容を見て判断できるようにする)
6. 上記25銘柄のローテーションと別に、「株主優待 変更 廃止 新設」等で
   直近1〜2週間のニュースを2〜3回検索し、YUTAI_LIST内の銘柄に該当する
   変更が無いか確認する(247銘柄を1件ずつ検索するより効率的)
7. `approxInvestment` が前回値から30%以上変動した銘柄は、報告で目立たせる
   (異常ではなく、気づいてほしい事実として報告するのみ。止めない)

## 仕上げ

1. `npm run lint && npm run build`
2. dev サーバーで `/results` が正常に表示されることを確認
3. Playwrightは使わない。lint/build/curlまで。
   入れたらコミット前に必ず `npm uninstall playwright`、`package.json`/`package-lock.json` を戻す。
4. `git add`(yutai-data.ts とこのファイルのみ) &&
   `git commit -m "chore: 週次データメンテナンス(YYYY-MM-DD)"` && `git push`

## 報告フォーマット

- 更新した25銘柄のリスト(銘柄名・旧/新 approxInvestment・lastVerified)
- 30%以上の変動があった銘柄(あれば)
- 優待制度に変更があった銘柄(あれば)
- 廃止が疑われる銘柄(あれば、dataQuality 変更の詳細)
- 次回(来週)更新すべき25銘柄(lastVerified が古い順の次の25件)の予告

## ローテーション記録

| 実施日 | 対象銘柄(先頭コード〜末尾コード) | 備考 |
|--------|----------------------------------|------|
| 2026-06-25 | 4755(楽天)〜8011(三陽商会) | 初回。EC・通信・外食・ファッション計25銘柄 |
